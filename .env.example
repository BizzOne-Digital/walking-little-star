# Form submission endpoint
# Set this to your form handling service URL (e.g. Formspree, Netlify Forms, etc.)
# Without this, the form will show a message directing users to call or email directly
VITE_FORM_ENDPOINT=
